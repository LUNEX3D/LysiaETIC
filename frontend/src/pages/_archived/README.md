# Archived UI (deprecated)

Bu klasördeki sayfalar **yeni rotalarda kullanılmıyor**; yalnızca referans veya geçmiş diff için tutulur.

Yeni geliştirmeleri bu dosyalara değil, güncel `pages` bileşenlerine yapın. Üretim build’ine dahil edilmemeleri için `App.js` / router’da bu path’lere route eklemeyin.
